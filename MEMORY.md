# Agent Memory

## Project Context
This project is a DevOps Diploma repository containing multiple training sessions, including DevOps Mindset, Git, Linux, Docker, AWS Core Services, AWS DevOps (HA & Scaling), GitHub Actions & Workflows, and Terraform.

## Current Objective
Consolidate all labs and assignments starting from Session 1 (DevOps Mindset) up to Session 9 (Terraform) into a unified `all-labs/` directory, copying original instructions and providing fully functional code/configurations and comprehensive solutions for every single lab and assignment.

## State and History
- **2026-06-14:** Initialized the consolidation task. Approved the implementation plan and task list. Solved Topic 1 (DevOps Mindset), Topic 2 (Git Prep), Topic 3 (Linux & Docker Intro), Topic 4 (Docker Deep Dive), Topic 5 (Git Advanced), and Topic 6 (AWS Core Services).
- **2026-06-14 (Continued):** Solved Topic 7 (AWS DevOps HA & Scaling), migrated Topic 8 (GitLab CI -> GitHub Actions & Workflows), and solved Topic 9 (Terraform Modules & Auto Scaling Raw HCL codebase). Task checklist fully completed.
- **2026-06-14 (Slide Update):** Expanded the Terraform Session 2 presentation (`presentation.md` and `presentation.html`) with 3 new slides detailing variable types (primitives, collections, maps/objects), HCL declarations, and referencing syntax. Compiled the updated presentation deck into a PDF named `Modules , Variables and Loops.pdf`.
- **2026-06-16:** Enhanced the midterm exam question bank (`midterm_questions_bank.md`) to align all questions directly with the taught course labs and concepts, replacing file path details and academic questions (like git config paths and Copy-on-Write) with practical levels and commands.
- **2026-06-16 (Formatting Update):** Formatted the midterm exam question bank (`midterm_questions_bank.md`) so that correct answer choices are bolded directly in the lists rather than being written separately under the choices, making the question bank much cleaner to review.
- **2026-06-16 (Shuffling & Category Removal):** Shuffled all 130 questions in the midterm exam question bank (`midterm_questions_bank.md`) randomly and renumbered them sequentially. Removed all category-specific subheadings and the Table of Contents, leaving a single, unified list of questions with only the document title.
- **2026-06-16 (Circle Formatting):** Positioned a solid circle symbol `●` at the end of the line inside the bold markers for each correct answer choice in `midterm_questions_bank.md`.
- **2026-06-16 (Triangle & PDF Compilation):** Updated all 130 correct options inside `midterm_questions_bank.md` to display a solid triangle symbol `▲` at the end instead of the circle `●`. Compiled the updated question bank to a professionally designed PDF named `midterm_questions_bank.pdf` at the workspace root using headless Chrome print features.
- **2026-06-16 (Q5 Replacement):** Replaced the deleted Question 5 with a new practical question about AWS NAT Gateways, maintaining the total count at 130 questions, and regenerated `midterm_questions_bank.pdf`.
- **2026-06-17:** Completed the MidTerm Project DevOps platform execution. Restructured repository layout into root `backend/` and `frontend/` folders, migrated Node application to Sequelize PostgreSQL (matching `_id` outputs for React frontend), containerized both services with Docker/Docker Compose, built Terraform multi-environment infra modules (VPC, RDS, ALB, IAM, EC2 ASG), and configured 4 GitHub Actions OIDC pipelines (Infra, Build with Trivy, Deploy with ASG Instance Refresh, and SonarQube).
- **2026-06-19:** Refactored Terraform codebase to use official and community-supported AWS Registry modules (VPC, ECR, RDS, ALB, ASG) and native IAM resources. Unified the configuration under a single codebase in `infra/`, using environment-specific variable files (`dev.tfvars` and `prod.tfvars`) instead of duplicate configurations. Updated the infrastructure CI/CD workflow to support the unified structure, committed and pushed changes to the `feat/infra-setup` branch.
- **2026-06-19 (Continued):** Added comprehensive line-by-line explanatory inline comments to all DevOps files, including Docker orchestration (`docker-compose.yml`, `backend/Dockerfile`, `frontend/Dockerfile`), Nginx routing (`frontend/nginx.conf`), Terraform configurations (`infra/main.tf`, `variables.tf`, `outputs.tf`, `providers.tf`, `versions.tf`, `user_data.sh`, `dev.tfvars`, `prod.tfvars`), and GitHub CI/CD workflows, providing detailed engineering rationale for every attribute and selection.
- **2026-06-19 (NAT Gateways disabled):** Reconfigured the VPC module inside `infra/main.tf` to disable NAT Gateways entirely (`enable_nat_gateway = false`), restricting the VPC to use only a single Internet Gateway as requested.
- **2026-06-19 (HTTP simplified):** Simplified the ALB configuration in `infra/main.tf` to only listen on HTTP port 80, removing the merge functions, ternary operators, port 443 listeners, and duplicate listener rules, making HTTP-only routing cleaner and simpler.
- **2026-06-19 (Public-Only Subnets):** Migrated the entire resource stack (RDS and ASG) in `infra/main.tf` to deploy inside the VPC's public subnets. Disables private subnets creation in the VPC module, and enables `map_public_ip_on_launch = true` to automatically assign public IPs to virtual machines on boot.
- **2026-06-19 (EC2 IAM simplified):** Removed the commented-out SSM managed instance core attachment and deleted `ec2_custom_policy` (along with its role attachment mapping) from `infra/main.tf`. The EC2 instance role is now limited strictly to the required read-only ECR permissions (`AmazonEC2ContainerRegistryReadOnly`) to pull container images.
- **2026-06-19 (Static AWS Credentials):** Completely deleted OIDC and GitHub Actions IAM role/policy configurations from `infra/main.tf` and `variables.tf`. Reconfigured GitHub Actions workflow files (`infra.yml`, `build.yml`, and `deploy.yml`) to authenticate using static AWS secrets (`AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`) and simplified permission mappings by removing the dynamic JWT `id-token` write permission.
- **2026-06-19 (EC2 IAM Roles removed):** Removed all EC2 IAM roles, policy attachments, and instance profile configurations from `infra/main.tf` entirely. Added `aws_access_key_id` and `aws_secret_access_key` variables in `variables.tf` and passed them down to `infra/user_data.sh` via the ASG module's user data template to authenticate local ECR pulls without needing EC2 roles.
- **2026-06-19 (Orphaned IAM Cleanup):** Cleaned up `infra/outputs.tf` by removing the `github_actions_role_arn` output. Confirmed that no other IAM resources, roles, or instance profiles remain in the Terraform configurations on disk.
- **2026-06-19 (Secrets Manager Integration):** Integrated AWS Secrets Manager to retrieve database credentials dynamically in Terraform, decoding the JSON secret string to fetch the username and password. Removed manual `db_user` and `db_password` variables from codebases, environments tfvars, and workflows. Cleared out unused `private_subnets` configuration.
- **2026-06-19 (.gitignore added):** Added a global `.gitignore` file to the root of the workspace to avoid tracking node modules, local env files, and Terraform state caches.
- **2026-06-19 (JWT Secret Integrated):** Migrated `jwt_secret` configuration into the AWS Secrets Manager JSON payload, retrieving and decoding it dynamically inside Terraform to boot the ASG instances, removing manual parameters.
- **2026-06-19 (Local S3 Backends):** Created S3 backend configuration files (`dev.tfbackend` and `prod.tfbackend`) to dynamically configure partial backend parameters (`midterm-project-state-s3` in `eu-west-1`) locally. Updated all variables tfvars environments region allocations to `eu-west-1` and availability zones `["eu-west-1a", "eu-west-1b"]`.
- **2026-06-20 (ALB v9 Compatibility Fix):** Updated the ALB module in `infra/main.tf` to resolve `target_id` and security group rule attribute errors (v9 compatibility). Disabled static attachments and updated rules to use `cidr_ipv4` and `ip_protocol`. Verification plan ran successfully.
